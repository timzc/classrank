services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    env_file:
      - .env
    environment:
      - DEBUG=False
      - DB_DIR=/app/data
    restart: unless-stopped

volumes:
  db_data:
